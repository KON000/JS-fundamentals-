const age = 16;
if(age >= 20) {  // A
    console.log('true');
}
    else(age < 30) {  // B
        console.log('false')
    }

    // not A = true
    // A and B = false
    // A or B = true
    // not A and B = true
    // A or !B = false